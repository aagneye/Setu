export function GoogleAuthPlaceholder() {
  return (
    <div className="rounded-md border border-dashed p-3 text-xs text-muted-foreground">
      Google Auth will be wired in next step (you can follow `docs/setup.md`).
    </div>
  );
}
